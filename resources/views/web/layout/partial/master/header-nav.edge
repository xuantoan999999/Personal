<header id="mod-header">
    <div class="header-nav" :class="classHeader">
        <div class="container">
            <a href="/" class="button
            @if(activePage == 'home')
            active
            @endif
            ">
                <strong>Home</strong>
            </a>
            <a href="/convert-image" class="button
            @if(activePage == 'tinyPNG')
            active
            @endif
            ">
                <strong>Image manager</strong>
            </a>
        </div>
    </div>
</header>